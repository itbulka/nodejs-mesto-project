const reqError: number = 400;
const authError: number = 401;
const notPermission: number = 403;
const notFoundError: number = 404;
const reCreateError: number = 409;
const defaultError: number = 500;

export default {
  reqError, authError, notPermission, notFoundError, defaultError, reCreateError,
};
